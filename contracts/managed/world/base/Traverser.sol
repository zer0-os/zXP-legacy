// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract Traverser{
    uint currentWorld;

    function traverse(uint portal) public{
        //exitPortal(currentWorld);
        //enterPortal(portal);
    }
}