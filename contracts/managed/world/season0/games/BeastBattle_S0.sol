pragma solidity ^0.8.14;

import "../RegistryClient.sol";    
import "../base/IZXP.sol";

contract BeastBattle_S0 is RegistryClient{
     constructor(IRegistry registry) RegistryClient(registry) {}

     function battle() public {

     }
}